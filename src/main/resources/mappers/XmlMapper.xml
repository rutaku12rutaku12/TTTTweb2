<?xml version="1.0" encoding="UTF-8" ?>
<!-- XML 이란? 마크업 이용한 데이터 저장(설정)/전달(매핑)/교환(API) 마크업언어 -->
<!-- HTML : 마크업 이용한 웹페이지 마크업언어 -->

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- XML 파일내 mybatis 라이브러리 설정 -->

<!-- xml 과 매핑할 인터페이스 설정 , [src] 이하 경로 지정 -->
<!-- <mapper namespace="매핑할 인터페이스 파일 경로"> -->
<mapper namespace="example.day13.XmlMapper"> <!-- mapper 시작 -->
    <!-- [1] 등록 -->
    <!-- <insert id="추상메소드명"> parameterType="매개변수타입경로(클래스)" -->
    <insert id="save" parameterType="example.day06.StudentDto" useGeneratedKeys="true" keyProperty="sno">
        INSERT INTO student(name, kor, math)
        values( #{name} , #{kor}, #{math} )
    </insert>

    <!-- [2] 전체조회 -->
    <!-- <select id="findAll" resultType="반환타입 파일경로"> -->
    <select id="findAll" resultType="example.day06.StudentDto">
        SELECT * from student
    </select>

    <!-- [3] 개별조회 -->
    <select id="find" parameterType="int" resultType="example.day06.StudentDto">
        SELECT * FROM student where sno = #{sno};
    </select>

    <!-- [4] 개별삭제 : 반환타입 설정하지않고 int 일경우에는 자동으로 삭제/수정/삽입 된 레코드수 반환 -->
    <delete id="delete" parameterType="int" >
        DELETE FROM student
        WHERE sno = #{sno};
    </delete>

    <!-- [5] 개별수정 -->
    <update id="update" parameterType="example.day06.StudentDto">
        UPDATE student SET name=#{name}, kor = #{kor}, math = #{math} WHERE sno= #{sno}
    </update>
</mapper> <!-- mapper 끝 -->
